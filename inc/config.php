<?php
    // Metadata
    $title_tag = " | Fotovereniging ISO'73"; // Tag at end of title
    
    // Database
    $db_config = array(
        'DB_HOST'=>'localhost',
        'DB_USERNAME'=>'root',
        'DB_PASSWORD'=>'root',
        'DB_DATABASE'=>'iso73'
    );

    // Menu
    $menu_items = array('Home', 'Portfolio', 'Over ons', 'Contact'); // Menu items
    $menu_item_bordered = 'Mijn account'; // Bordered menu item

    // Global variables
?>